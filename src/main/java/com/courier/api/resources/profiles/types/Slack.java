/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.courier.api.resources.profiles.types;

import com.courier.api.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import java.io.IOException;
import java.util.Objects;

@JsonDeserialize(using = Slack.Deserializer.class)
public final class Slack {
    private final Object value;

    private final int type;

    private Slack(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((SendToSlackChannel) this.value);
        } else if (this.type == 1) {
            return visitor.visit((SendToSlackEmail) this.value);
        } else if (this.type == 2) {
            return visitor.visit((SendToSlackUserId) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Slack && equalTo((Slack) other);
    }

    private boolean equalTo(Slack other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static Slack of(SendToSlackChannel value) {
        return new Slack(value, 0);
    }

    public static Slack of(SendToSlackEmail value) {
        return new Slack(value, 1);
    }

    public static Slack of(SendToSlackUserId value) {
        return new Slack(value, 2);
    }

    public interface Visitor<T> {
        T visit(SendToSlackChannel value);

        T visit(SendToSlackEmail value);

        T visit(SendToSlackUserId value);
    }

    static final class Deserializer extends StdDeserializer<Slack> {
        Deserializer() {
            super(Slack.class);
        }

        @java.lang.Override
        public Slack deserialize(JsonParser p, DeserializationContext ctxt) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, SendToSlackChannel.class));
            } catch (IllegalArgumentException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, SendToSlackEmail.class));
            } catch (IllegalArgumentException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, SendToSlackUserId.class));
            } catch (IllegalArgumentException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
