/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.courier.api.resources.audiences.types;

import com.courier.api.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import java.io.IOException;
import java.util.Objects;

@JsonDeserialize(using = Filter.Deserializer.class)
public final class Filter {
    private final Object value;

    private final int type;

    private Filter(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((SingleFilterConfig) this.value);
        } else if (this.type == 1) {
            return visitor.visit((NestedFilterConfig) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Filter && equalTo((Filter) other);
    }

    private boolean equalTo(Filter other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static Filter of(SingleFilterConfig value) {
        return new Filter(value, 0);
    }

    public static Filter of(NestedFilterConfig value) {
        return new Filter(value, 1);
    }

    public interface Visitor<T> {
        T visit(SingleFilterConfig value);

        T visit(NestedFilterConfig value);
    }

    static final class Deserializer extends StdDeserializer<Filter> {
        Deserializer() {
            super(Filter.class);
        }

        @java.lang.Override
        public Filter deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, SingleFilterConfig.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, NestedFilterConfig.class));
            } catch (RuntimeException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
